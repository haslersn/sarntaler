/*
 * Example implementation of
 * the example contract of
 * issue #26.
 */
contract {
    int public_key;
}

int check_key_pair(int pub,int priv){
    // inline LABVM
}

int unlock(int private_key) {
    if (check_key_pair(public_key, private_key)==0) {
        transfer(msg,contract.balance);
    }
}